import 'package:flutter/material.dart';

class ImagePickerBody extends StatelessWidget {
  const ImagePickerBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
